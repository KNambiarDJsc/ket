# Example DB App — Requirements

- Deleted projects must be permanently removed from the database, not merely hidden.
- The service must expose a health check at GET /.
- A duplicated project-creation request must not create two projects.
